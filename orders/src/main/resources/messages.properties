# ==============================
# OrderRequestDto validations
# ==============================
order.customerId.required=Customer ID is required.
order.customerId.positive=Customer ID must be positive.

order.paymentInfo.required=Payment info is required.

order.orderItems.required=Order items are required.
order.orderItems.notEmpty=Order must contain at least one item.

# ==============================
# PaymentInfoDto validations
# ==============================
order.paymentInfo.metadata.required=Payment metadata is required.
order.paymentInfo.metadata.size=Payment metadata must be at most 255 characters.

order.paymentInfo.paymentType.required=Payment type is required.
order.paymentInfo.paymentType.invalid=Invalid payment type.

# ==============================
# OrderItemDto validations
# ==============================
order.orderItems.productId.required=Product ID is required.
order.orderItems.productId.positive=Product ID must be positive.

order.orderItems.amount.required=Product amount is required.
order.orderItems.amount.positive=Product amount must be greater than zero.
order.orderItems.amount.check=Product amount must comply with database constraint (>0).

order.orderItems.unitPrice.required=Unit price is required.
order.orderItems.unitPrice.positive=Unit price must be greater than or equal to zero.
order.orderItems.unitPrice.check=Unit price must comply with database constraint (>=0).

# ==============================
# Optional fields validations
# ==============================
order.observations.size=Observations must be at most 1000 characters.

order.invoiceUrl.size=Invoice URL must be at most 1000 characters.

order.paymentKey.size=Payment key must be at most 1000 characters.

# ==============================
# Order status validations
# ==============================
order.status.invalid=Invalid order status. Must be one of: PLACED, PAID, BILLED, SHIPPED, PAYMENT_ERROR, PREPARING_SHIPMENT.

# ==============================
# Tracking code / UUID
# ==============================
order.trackingCode.invalid=Tracking code must be a valid UUID.

# ==============================
# Total validations
# ==============================
order.total.required=Order total is required.
order.total.positive=Order total must be greater than or equal to zero.

# ==============================
# PaymentRequestDto validations
# ==============================
order.id.required=Order ID is required
order.id.positive=Order ID must be positive